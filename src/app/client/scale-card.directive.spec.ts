import { ScaleCardDirective } from './scale-card.directive';

describe('ScaleCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleCardDirective();
    expect(directive).toBeTruthy();
  });
});
