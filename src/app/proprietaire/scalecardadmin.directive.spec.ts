import { ScalecardadminDirective } from './scalecardadmin.directive';

describe('ScalecardadminDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalecardadminDirective();
    expect(directive).toBeTruthy();
  });
});
